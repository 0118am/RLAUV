# Isaac Lab

Human-facing experiment entry points are `notebooks/train.ipynb` and
`notebooks/evaluate.ipynb`. Script entry points live under `workflows/`, PPO and
reward code under `agents/`, the PID baseline under `controllers/`, and the
Isaac/PhysX task adapter under `envs/auv/`.
