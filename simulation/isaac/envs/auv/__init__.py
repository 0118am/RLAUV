"""AUV environment package.

The initializer stays import-light so shared environment and robot modules can
be used without starting IsaacLab. Task registration imports ``config`` and
``env`` explicitly.
"""
