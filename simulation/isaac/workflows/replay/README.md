# Replay workflows

Use `play_pool_action_replay.py` to produce an Isaac replay from measured
actions. `run_pool_action_replay_checked.py` adds process/artifact checks;
`validate_pool_replay.py` compares measured and simulated logs, and
`summarize_pool_replay_validation.py` aggregates held-out cases.
